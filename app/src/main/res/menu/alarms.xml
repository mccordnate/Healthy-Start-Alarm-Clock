<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".AlarmsActivity" >
    <item android:id="@+id/action_change_age"
        android:title="@string/action_change_age"
        android:orderInCategory="100"
        app:showAsAction="never" />
    <item android:id="@+id/action_add_alarm"
        android:title="@string/add_new_alarm"
        android:orderInCategory="1"
        app:showAsAction="ifRoom"
        android:icon="@drawable/ic_action_add_alarm"/>
</menu>
